[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coarse-ink"
version = "1.3.0"
description = "Free, open-source AI academic paper reviewer. BYOK — bring your own key."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [{ name = "David Van Dijcke" }]
keywords = ["academic", "paper", "review", "ai", "llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "litellm>=1.83.0",
    "instructor>=1.15.1",
    "pydantic>=2.0",
    "typer>=0.12",
    "rich>=13.0",
    "tomli-w>=1.0",
    "pymupdf>=1.24",
    "python-dotenv>=1.0",
    "requests>=2.31",
    # Mozart fork: docling promoted from optional extra to main dependency.
    # Lets headless users (e.g. `--ocr-backend docling` runs that skip
    # OpenRouter entirely) work out of the box without `uvx --with docling`.
    # Adds ~120 packages including torch/transformers — heavy but justified
    # for the fork's primary use case (subscription-only, no API keys).
    "docling>=2.86.0",
    "transformers>=5.4,<6",
]

[project.urls]
Homepage = "https://github.com/Davidvandijcke/coarse"
Repository = "https://github.com/Davidvandijcke/coarse"
Issues = "https://github.com/Davidvandijcke/coarse/issues"
Changelog = "https://github.com/Davidvandijcke/coarse/blob/main/CHANGELOG.md"

[project.optional-dependencies]
docling = ["docling>=2.86.0", "transformers>=5.4,<6"]
formats = ["mammoth>=1.6", "markdownify>=0.12", "ebooklib>=0.18"]
all = ["coarse-ink[docling]", "coarse-ink[formats]"]
dev = ["pytest>=8.0", "ruff>=0.4"]

[project.scripts]
coarse = "coarse.cli:app"
coarse-ink = "coarse.cli:app"
coarse-review = "coarse.cli_review:main"

[tool.hatch.build.targets.wheel]
packages = ["src/coarse"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-m 'not slow'"
markers = ["slow: marks tests as slow (deselect with '-m \"not slow\"')"]
